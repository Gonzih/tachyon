name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Install cognitive-complexity gate
        run: brew install fummicc1/tap/swift-complexity
      - name: Verify
        run: ./verify.sh
